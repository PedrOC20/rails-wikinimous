<h2>Lisf of Articles</h2>

<ul>
  <% @articles.each do |article| %>
    <li>
      <h3><%= link_to "#{article.title}", article %></h3>
      <p><%= link_to 'Delete Article', article, method: :delete %></p>
    </li>
  <% end %>
</ul>

<%= link_to 'Add new Article', new_article_path %>
